<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.oauth.mapper.MemberMapper">

<!--    <insert id="insert" parameterType="MemberVO">-->
<!--        <selectKey order="BEFORE" resultType="Long" keyProperty="id">-->
<!--            SELECT SEQ_OAUTH_MEMBER.NEXTVAL FROM DUAL;-->
<!--        </selectKey>-->
<!--        INSERT INTO TBL_OAUTH_MEMBER-->
<!--        VALUES (-->
<!--                #{id},-->
<!--                #{memberEmail},-->
<!--                <choose>-->
<!--                    <when test="memberPassword != null">#{memberPassword}</when>-->
<!--                    <otherwise>NULL</otherwise>-->
<!--                </choose>,-->
<!--                <choose>-->
<!--                    <when test="memberPicture != null">#{memberPicture}</when>-->
<!--                    <otherwise>DEFAULT</otherwise>-->
<!--                </choose>-->
<!--                #{memberName},-->
<!--                #{memberNickname},-->
<!--                <choose>-->
<!--                    <when test="memberProvider != null">#{memberProvider}</when>-->
<!--                    <otherwise>DEFAULT</otherwise>-->
<!--                </choose>-->
<!--                )-->
<!--    </insert>-->

    <insert id="insert" parameterType="MemberVO">
        <selectKey resultType="Long" keyProperty="id" order="BEFORE" >
            SELECT SEQ_MEMBER.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO TBL_MEMBER
        VALUES(#{id}, #{memberEmail}. #{memberPassword}, #{memberPicture}, #{memberName}, #{memberNickname})
    </insert>

    <select id="select" parameterType="Long" resultType="MemberVO">
        SELECT ID, MEMBER_EMAIL, MEMBER_PASSWORD, MEMBER_PICTURE, MEMBER_NAME, MEMBER_NICKNAME
        FROM TBL_MEMBER
        WHERE ID = #{id}
    </select>

    <select id="selectByMemberEmail" parameterType="String" resultType="MemberVO">
        SELECT ID, MEMBER_EMAIL, MEMBER_PASSWORD, MEMBER_PICTURE, MEMBER_NAME, MEMBER_NICKNAME
        FROM TBL_MEMBER
        WHERE MEMBER_EMAIL = #{memberEmail}
    </select>

    <select id="existsMemberByMemberEmail" parameterType="String" resultType="_boolean">
        SELECT
            CASE
                WHEN COUNT(ID) > 0
                THEN 1
                ELSE 0
            END
        FROM TBL_MEMBER
        WHERE MEMBER_EMAIL = #{memberEmail}
    </select>

    <update id="update" parameterType="MemberVO">
        UPDATE TBL_OAUTH_MEMBER
        SET MEMBER_EMAIL = #{memberEmail} , MEMBER_PASSWORD = #{memberPassword} , MEMBER_PICTURE=#{memberPicture}, MEMBER_NAME=#{memberName}, MEMBER_NICKNAME = #{memberNickname}, MEMBER_PROVIDER = #{memberProvider}
        WHERE ID = #{id}
    </update>

    <delete id="delete" parameterType="Long">
        DELETE FROM TBL_MEMBER
        WHERE ID = #{id}
    </delete>
</mapper>